import { ExcelDatePipe } from './excel-date.pipe';

describe('ExcelDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ExcelDatePipe();
    expect(pipe).toBeTruthy();
  });
});
